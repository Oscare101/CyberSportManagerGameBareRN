import {StyleSheet, Text, View} from 'react-native';
import React, {memo} from 'react';

function PlayersPage() {
  return (
    <View>
      <Text>PlayersPage</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
export default memo(PlayersPage);
